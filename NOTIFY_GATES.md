# 通知ゲート（再編成・2026-09-17）

**主軸は流動性・ローンチ窓。スマートウォレットは補助。**

## ハード（見送り）
1. **流動性** — `MIN_LIQ_USD`（$2500）+ `LIQ_MCAP_MIN`（15%）
2. **ローンチ窓** — 新しすぎ `MIN_TOKEN_AGE_SEC`（15分）／古すぎ `MAX_TOKEN_AGE_SEC`（6時間）
3. **急騰上限** — 5分・1時間のスパイク上限（従来）
4. **最低交差** — 監視財布 ≥2、買い合計 `MIN_CLUSTER_USD`（$150）

## ソフト（既定オフ＝見送りにしない）
- 仕込み必須 / early-only リスト / 財布質スコア
- 買い増加必須・上下動必須・出来高欠落
- 卒業前は 5分出来高が極端に薄いときだけ抑制

## 補助
- bot疑惑ラベルは交差から除外（残数が足りなければ見送り）
- `@xbtscout` 投稿直前買いは RH 監視にマージ継続
